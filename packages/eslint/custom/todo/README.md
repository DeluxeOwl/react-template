This is inspired by https://github.com/shepherdjerred/scout-for-lol/tree/main/eslint-rules

It also has a rule for jscpd (to run as eslint plugin). And tests.

However, it's _very slow_ so it's disabled.

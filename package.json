{
  "name": "react-template",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "scripts"
  ],
  "type": "module",
  "devDependencies": {
    "@total-typescript/ts-reset": "^0.6.1",
    "@types/bun": "latest",
    "oxlint": "^1.38.0"
  },
  "scripts": {
    "lint:oxlint": "bun run --filter '*' lint:oxlint"
  }
}